package cc3002.effect;

import cc3002.Trainer;
import cc3002.energyCost.EnergyCost;

public class EnergyBurn extends AbstractEffect{

    @Override
    public void doEffect() {
        /*int value = 0;
        for (String i:trainer.getActivePokemon().getCountEnergy().getCost().keySet()) {
            value += trainer.getActivePokemon().getCountEnergy().getCost().get(i);
        }
        EnergyCost newEnergy = new EnergyCost(0,0,0,0,0,0);
        newEnergy.getCost().put("water",value);
        trainer.getActivePokemon().setCountEnergy(newEnergy);

*/    }



}
